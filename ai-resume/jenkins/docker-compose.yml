services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    container_name: resume-ai-jenkins
    restart: unless-stopped
    user: root
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ./jenkins_home:/var/jenkins_home
      - /etc/localtime:/etc/localtime:ro
